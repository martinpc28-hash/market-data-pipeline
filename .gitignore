# Nunca subir credenciales
.env
secrets/
*.pem
*.key

# Datos capturados (pueden pesar mucho, no van al repo)
data/
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Rust (rust-feed/) -- build artifacts, se regeneran con cargo build
rust-feed/target/
